    <div>
      <h3><em><span id="track_info"></span></em></h3>

      <table class="table text-center">
        <tr>
          <td></td>
          <td>
            <a class="btn btn-inverse btn-default btn-lg player" id="volume_up">
              <span title="volume up" class="glyphicon glyphicon-volume-up" aria-hidden="true"></span>
            </a>
          </td>
          <td></td>
        </tr>

        <tr>
          <td>
            <a class="btn btn-lg btn-primary player" id="player_prev_track" role="button">
              <span title="previous track" class="glyphicon glyphicon-backward" aria-hidden="true"></span>
            </a>
          </td>
          <td>

            <a class="btn btn-lg btn-danger player" id="player_pause" role="button" style="display: none;">
              <span title="pause" class="glyphicon glyphicon-pause" aria-hidden="true"></span>
            </a>

            <a class="btn btn-lg btn-danger player" id="player_play" role="button">
              <span title="play" class="glyphicon glyphicon-play" aria-hidden="true"></span>
            </a>

          </td>
          <td>
            <a class="btn btn-lg btn-primary player" id="player_next_track" role="button">
              <span title="next track" class="glyphicon glyphicon-forward" aria-hidden="true"></span>
            </a>
          </td>
        </tr>

        <tr>
          <td></td>
          <td>
            <a class="btn btn-inverse btn-default btn-lg player" id="volume_down">
              <span title="volume down" class="glyphicon glyphicon-volume-down" aria-hidden="true"></span>
            </a>
          </td>
          <td></td>
        </tr>
      </table>

      <div class="progress">
        <div id="volume_value" class="progress-bar" role="progressbar" aria-valuenow="100" aria-valuemin="0" aria-valuemax="100" style="width: 100%;">
          Volume 100%
        </div>
      </div>

      <div class="text-center" id="lyrics">
        <a class="btn btn-inverse btn-default btn-lg" id="show_lyrics">
          <span title="lyrics" class="glyphicon glyphicon-align-left" aria-hidden="true"></span> lyrics
        </a>
        <br><br>
        <div id="lyrics_text" style="display: none;">
        </div>
      </div>
    </div>

<script>
  function go_90s(title) {
    return '<marquee scrollamount="10">' + title + '</marquee>';
  }

  function switch_to_play(playing) {
    if (playing) {
      $('#player_pause').show();
      $('#player_play').hide();

      // change title
      document.title = 'rubomote 🔈🎶'
    } else {
      $('#player_pause').hide();
      $('#player_play').show();

      document.title = 'rubomote 🔈'
    }
  }

  $(document).ready(function(){
    show_lyrics = false;
    song_title = '';

    // when browser window is on focus request track name and volume
    window.onfocus = function () {
      ws.send('status');
    };

    window.onload = function() {
      //create a new instance of shake.js.
      var myShakeEvent = new Shake({
        threshold: 15
      });
      // start listening to device motion
      myShakeEvent.start();
      // register a shake event
      window.addEventListener('shake', shakeEventDidOccur, false);
      //shake event callback
      function shakeEventDidOccur () {
        // when smartphone is shaking go to next track
        ws.send('player_next_track');
      }
    };

    // web socket stuff

    var ws = new WebSocket('ws://' + window.location.host + '/auth/<%= Base64.urlsafe_encode64(settings.session_secret) %>');
    ws.onopen  = function() { console.log('successfully connected to WebSocket server!'); };
    ws.onclose = function() { console.log('WebSocket server went down!'); location.reload(); };
    ws.onmessage = function(m) {
      console.log('data received: ' + m.data);

      if (m.data == 'pause') {
        switch_to_play(false);
      } else if (isFinite(String(m.data))) { // check if incoming data is a number
        $('#volume_value').attr('aria-valuenow', m.data);
        $('#volume_value').attr('style', 'width: ' + m.data + '%;');
        $('#volume_value').html('Volume ' + m.data + '%');
      } else if (m.data.startsWith("lyrics_")) {
        // if incoming data from server starts with string "lyrics_" put
        // lyrics in div lyrics_text

        lyrics_content = m.data.replace(/lyrics_/g,'');

        $('#lyrics').attr('style', 'display: block;');
        $('#lyrics_text').html('<pre>' + lyrics_content + '</pre><h4>downloaded from <a href="https://genius.com" target="_blank">genius.com</a></h4>');
      } else {
        // if song changed then remove the lyrics and set the new title
        // on top of the page

        if (song_title != m.data) {
          $('#lyrics_text').html('<pre>loading...</pre>');
          $('#lyrics_text').attr('style', 'display: none;');

          show_lyrics = false;
          song_title = m.data;

          // incoming data is a string so it is interpreted as track name
          $('#track_info').html(go_90s(m.data));
        }

        switch_to_play(true);
      }
    }

    // button events

    $('#show_lyrics').click(function(){
      show_lyrics = !show_lyrics;

      if (show_lyrics) {
        $('#lyrics_text').attr('style', 'display: block;');
        ws.send('lyrics');
      } else {
        $('#lyrics_text').attr('style', 'display: none;');
      }
    });

    $(".player").click(function(){
      // when clicked volume up button and volume is already to max then do not
      // send request to the server, same thing for volume down button

      if (!(this.id == 'volume_up' && $('#volume_value').text() == 'Volume 100%') &&
          !(this.id == 'volume_down' && $('#volume_value').text() == 'Volume 0%')) {
        ws.send(this.id);
      }
    });
  });
</script>
